# Active Plan

At most one implementation plan belongs in this directory.

When this directory contains a plan, agents must continue or explicitly resolve that plan before
starting another milestone. See `plans/README.md` for the lifecycle and status rules.
